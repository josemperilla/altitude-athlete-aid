# Spotify (Authorization Code + PKCE) — crea una app en https://developer.spotify.com/dashboard
# y registra el redirect URI abajo (uno por entorno: localhost en dev, tu dominio en prod).
VITE_SPOTIFY_CLIENT_ID=
VITE_SPOTIFY_REDIRECT_URI=http://localhost:3000/spotify-callback
