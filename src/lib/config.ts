// Constantes de producto que no vienen de ningún backend.

/** La carrera objetivo del bloque actual (ISO, fecha local). */
export const RACE_DATE = "2026-10-04";
export const RACE_NAME = "Media Maratón Bogotá";

/** Longitud del bloque de preparación, para la barra de progreso hacia la carrera. */
export const BLOCK_WEEKS = 8;

/** Contexto del atleta, usado en cabeceras y meta. */
export const ALTITUDE_LABEL = "BOGOTÁ · 2.600 M";
